# Gigachat - AI Персональный Помощник

Интерактивный персональный помощник на Rust с интегрированным Sber GigaChat API.

## Демонстрация

<video src="assets/demo.mp4" controls width="70%"></video>

> Примечание: Видео показывает основные возможности приложения - взаимодействие с Clippy, открытие чата, виджеты погоды и валют.

## Возможности

### Основные функции
- **Взаимодействие с ИИ через GigaChat** - основной AI провайдер
- **Fallback механизм** - автоматическое переключение на OpenAI или Local если GigaChat недоступен
- **Информация о погоде** - получение и отображение погодных условий
- **Курсы валют** - текущие курсы USD и EUR к RUB
- **Голосовой вывод** - озвучивание ответов ИИ через Google Cloud TTS
- **Интерактивное окно** - всплывающий помощник поверх других окон

### Управление интерфейсом
- **Кнопка "+"** (синий кружок) - открыть облако с сообщением
- **Кнопка закрытия** (белый кружок) - закрыть облако
- **Двойной клик** на Clippy - открыть облако снова
- **Перетаскивание** Clippy по экрану

## 📁 Структура проекта

```
src/
├── ai/                      # AI провайдеры
│   ├── gigachat.rs         # Sber GigaChat API клиент
│   ├── openai.rs           # OpenAI API клиент (fallback)
│   ├── local.rs            # Локальные правила (fallback)
│   └── mod.rs              # Публичный API модуля
│
├── services/               # Внешние сервисы данных
│   ├── weather.rs          # Информация о погоде
│   ├── currency.rs         # Курсы валют
│   ├── storage.rs          # Хранилище разговоров (SQLite)
│   └── mod.rs              # Публичный API модуля
│
├── ui/                      # UI компоненты
│   ├── app.rs              # Основная логика приложения
│   ├── chat.rs             # Окно чата и сообщения
│   ├── chat_bubble.rs      # Речевой пузырь/облако
│   ├── buttons.rs          # Кнопки управления интерфейсом
│   ├── widgets.rs          # Виджеты (погода, валюты)
│   ├── visual.rs           # Визуальные эффекты и стили
│   ├── components.rs       # Переиспользуемые компоненты
│   └── mod.rs              # Публичный API модуля
│
├── core/                    # Основная логика
│   ├── agent.rs            # Router для AI провайдеров
│   ├── tts.rs              # Text-to-Speech интеграция
│   └── mod.rs              # Публичный API модуля
│
├── config.rs               # Загрузка конфигурации из .env
├── gui.rs                  # Главное окно приложения (egui)
├── logger.rs               # Инициализация логирования
└── main.rs                 # Точка входа

assets/
├── clippy.png             # Основная иконка помощника
└── clippy1.png            # Альтернативная иконка
```

## Быстрый старт

### Требования
- Rust 1.70+
- macOS / Linux / Windows

### Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/mlanies/gigachat
cd gigachat
```

2. Создайте `.env` файл:
```bash
# GigaChat API
GIGACHAT_API_KEY=ваш_ключ_здесь
GIGACHAT_MODEL=GigaChat:latest
GIGACHAT_TEMPERATURE=0.7
GIGACHAT_MAX_TOKENS=500

# OpenAI API (опционально, как fallback)
OPENAI_API_KEY=ваш_ключ_здесь
USE_OPENAI=false

# Google Cloud TTS (опционально)
GOOGLE_CLOUD_API_KEY=ваш_ключ_здесь
GOOGLE_CLOUD_PROJECT_ID=ваш_проект_здесь
```

3. Запустите приложение:
```bash
cargo run --release
```

## Конфигурация

### Переменные окружения

**GigaChat (основной AI):**
- `GIGACHAT_API_KEY` - API ключ Sber GigaChat
- `GIGACHAT_MODEL` - Модель (default: "GigaChat:latest")
- `GIGACHAT_TEMPERATURE` - Температура (0.0-1.0, default: 0.7)
- `GIGACHAT_MAX_TOKENS` - Макс. токенов (default: 500)

**OpenAI (fallback):**
- `OPENAI_API_KEY` - API ключ OpenAI
- `USE_OPENAI` - Включить OpenAI (default: false)

**Google Cloud (TTS):**
- `GOOGLE_CLOUD_API_KEY` - API ключ Google Cloud
- `GOOGLE_CLOUD_PROJECT_ID` - ID проекта

Все параметры загружаются из `.env` файла с использованием `dotenv`.

## Архитектура

### Приоритет AI провайдеров
```
1. GigaChat (основной)
   ↓ (если ошибка)
2. OpenAI (fallback)
   ↓ (если ошибка)
3. Local (встроенные правила)
```

### История разговора
- Максимум **10 сообщений** в памяти
- Автоматическое ограничение при превышении
- Можно очистить через метод `clear_history()`
- Сохранение в SQLite БД (файл `~/.config/clippy/clippy.db`)
- Уникальные сессии с автоматической генерацией ID

### Обработка ошибок
- Graceful fallback при недостижимости API
- Информативные сообщения об ошибках
- Логирование в stderr

## Разработка

### Добавление нового AI провайдера

1. Создайте файл `src/ai/provider_name.rs`
2. Реализуйте структуру с методом `get_response()`
3. Добавьте в `src/ai/mod.rs`
4. Обновите `src/core/agent.rs` для использования

### Добавление нового сервиса данных

1. Создайте файл `src/services/service_name.rs`
2. Реализуйте сервис
3. Добавьте в `src/services/mod.rs`
4. Используйте в агенте через `LocalAI` правила или в `agent.rs`

### Запуск тестов

```bash
cargo test
```

### Сборка release версии

```bash
cargo build --release
```

## Технический стек

- **Язык:** Rust 1.70+
- **GUI:** egui + eframe
- **HTTP:** reqwest
- **JSON:** serde + serde_json
- **Async:** tokio
- **Audio:** Google Cloud TTS (макOS: встроенный `say`)
- **Сериализация:** base64

## Контрибьютинг

Приветствуются pull requests! Убедитесь что:
- Код компилируется без ошибок (`cargo build`)
- Нет предупреждений (`cargo clippy`)
- Добавлены комментарии для сложных частей
- Файлы организованы по модулям

## Лицензия

[Apache License 2.0](LICENSE)

## Контакты

- GitHub Issues для багов и предложений
- Discussions для вопросов и идей

---

**Gigachat** - вдохновлен классическим Clippy из Microsoft Office, но современнее и мощнее! 🚀
